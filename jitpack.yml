# JitPack should use Java 21, as it's needed by Paper API
# Tests require it too, but JitPack skips them
before_install:
  - sdk install java 21-open
  - sdk use java 21-open
  - sdk install maven

jdk:
  - openjdk21
